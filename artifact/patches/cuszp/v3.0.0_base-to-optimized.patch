diff -ruN a/include/cuSZp/cuSZp_kernels_1D_f32.h b/include/cuSZp/cuSZp_kernels_1D_f32.h
--- a/include/cuSZp/cuSZp_kernels_1D_f32.h	2025-10-24 21:38:31.000000000 +0000
+++ b/include/cuSZp/cuSZp_kernels_1D_f32.h	2026-07-22 01:11:31.005422457 +0000
@@ -1,7 +1,8 @@
 #ifndef CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_1D_F32_H
 #define CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_1D_F32_H
 
-static const int tblock_size = 32; // Fixed to 32, cannot be modified.
+static const int warps_per_block = 8;
+static const int tblock_size = warps_per_block * 32;
 static const int thread_chunk = 1024;
 
 __global__ void cuSZp_compress_kernel_1D_fixed_f32(const float* const __restrict__ oriData, unsigned char* const __restrict__ cmpData, volatile unsigned int* const __restrict__ cmpOffset, volatile unsigned int* const __restrict__ locOffset, volatile int* const __restrict__ flag, const float eb, const size_t nbEle);
diff -ruN a/include/cuSZp/cuSZp_kernels_1D_f64.h b/include/cuSZp/cuSZp_kernels_1D_f64.h
--- a/include/cuSZp/cuSZp_kernels_1D_f64.h	2025-10-24 21:38:31.000000000 +0000
+++ b/include/cuSZp/cuSZp_kernels_1D_f64.h	2026-07-22 01:11:31.059378959 +0000
@@ -1,7 +1,8 @@
 #ifndef CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_1D_F64_H
 #define CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_1D_F64_H
 
-static const int tblock_size = 32; // Fixed to 32, cannot be modified.
+static const int warps_per_block = 8;
+static const int tblock_size = warps_per_block * 32;
 static const int thread_chunk = 1024;
 
 __global__ void cuSZp_compress_kernel_1D_fixed_f64(const double* const __restrict__ oriData, unsigned char* const __restrict__ cmpData, volatile unsigned int* const __restrict__ cmpOffset, volatile unsigned int* const __restrict__ locOffset, volatile int* const __restrict__ flag, const double eb, const size_t nbEle);
diff -ruN a/include/cuSZp/cuSZp_kernels_2D_f32.h b/include/cuSZp/cuSZp_kernels_2D_f32.h
--- a/include/cuSZp/cuSZp_kernels_2D_f32.h	2025-10-24 21:38:31.000000000 +0000
+++ b/include/cuSZp/cuSZp_kernels_2D_f32.h	2026-07-22 01:11:31.113487465 +0000
@@ -1,7 +1,8 @@
 #ifndef CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_2D_F32_H
 #define CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_2D_F32_H
 
-static const int tblock_size = 32; // Fixed to 32, cannot be modified.
+static const int warps_per_block = 8;
+static const int tblock_size = warps_per_block * 32;
 static const int block_per_thread = 16;
 
 // // To do: Implement the following kernels. (set as a placeholder, not sure if needed)
diff -ruN a/include/cuSZp/cuSZp_kernels_2D_f64.h b/include/cuSZp/cuSZp_kernels_2D_f64.h
--- a/include/cuSZp/cuSZp_kernels_2D_f64.h	2025-10-24 21:38:31.000000000 +0000
+++ b/include/cuSZp/cuSZp_kernels_2D_f64.h	2026-07-22 01:11:31.167807876 +0000
@@ -1,7 +1,8 @@
 #ifndef CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_2D_F64_H
 #define CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_2D_F64_H
 
-static const int tblock_size = 32; // Fixed to 32, cannot be modified.
+static const int warps_per_block = 8;
+static const int tblock_size = warps_per_block * 32;
 static const int block_per_thread = 32;
 
 __global__ void cuSZp_compress_kernel_2D_fixed_vec4_f64(const double* const __restrict__ oriData, unsigned char* const __restrict__ cmpData, volatile unsigned int* const __restrict__ cmpOffset, volatile unsigned int* const __restrict__ locOffset, volatile int* const __restrict__ flag, uint blockNum, const uint3 dims, const double eb);
diff -ruN a/include/cuSZp/cuSZp_kernels_3D_f32.h b/include/cuSZp/cuSZp_kernels_3D_f32.h
--- a/include/cuSZp/cuSZp_kernels_3D_f32.h	2025-10-24 21:38:31.000000000 +0000
+++ b/include/cuSZp/cuSZp_kernels_3D_f32.h	2026-07-22 01:11:31.221332828 +0000
@@ -1,7 +1,8 @@
 #ifndef CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_3D_F32_H
 #define CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_3D_F32_H
 
-static const int tblock_size = 32; // Fixed to 32, cannot be modified.
+static const int warps_per_block = 8;
+static const int tblock_size = warps_per_block * 32;
 static const int block_per_thread = 16;
 
 // // To do: Implement the following kernels. (set as a placeholder, not sure if needed)
diff -ruN a/include/cuSZp/cuSZp_kernels_3D_f64.h b/include/cuSZp/cuSZp_kernels_3D_f64.h
--- a/include/cuSZp/cuSZp_kernels_3D_f64.h	2025-10-24 21:38:31.000000000 +0000
+++ b/include/cuSZp/cuSZp_kernels_3D_f64.h	2026-07-22 01:11:31.274693997 +0000
@@ -1,7 +1,8 @@
 #ifndef CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_3D_F64_H
 #define CUSZP_INCLUDE_CUSZP_CUSZP_KERNELS_3D_F64_H
 
-static const int tblock_size = 32; // Fixed to 32, cannot be modified.
+static const int warps_per_block = 8;
+static const int tblock_size = warps_per_block * 32;
 static const int block_per_thread = 32;
 
 __global__ void cuSZp_compress_kernel_3D_fixed_vec4_f64(const double* const __restrict__ oriData, unsigned char* const __restrict__ cmpData, volatile unsigned int* const __restrict__ cmpOffset, volatile unsigned int* const __restrict__ locOffset, volatile int* const __restrict__ flag, uint blockNum, const uint3 dims, const double eb);
diff -ruN a/src/cuSZp_kernels_1D_f32.cu b/src/cuSZp_kernels_1D_f32.cu
--- a/src/cuSZp_kernels_1D_f32.cu	2025-10-24 21:38:31.000000000 +0000
+++ b/src/cuSZp_kernels_1D_f32.cu	2026-07-22 02:49:42.342982251 +0000
@@ -28,30 +28,27 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_1D_fixed_f32(const float* const __restrict__ oriData, 
-                                                unsigned char* const __restrict__ cmpData, 
-                                                volatile unsigned int* const __restrict__ cmpOffset, 
-                                                volatile unsigned int* const __restrict__ locOffset, 
-                                                volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_1D_fixed_f32(const float* const __restrict__ oriData,
+                                                unsigned char* const __restrict__ cmpData,
+                                                volatile unsigned int* const __restrict__ cmpOffset,
+                                                volatile unsigned int* const __restrict__ locOffset,
+                                                volatile int* const __restrict__ flag,
                                                 const float eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
     const float recipPrecision = 0.5f/eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
@@ -132,7 +129,7 @@
 
                 quant_chunk_idx = j * 32 + nbEle % 32;
                 for(int i=quant_chunk_idx; i<quant_chunk_idx+zeronbEle; i++) absQuant[i] = 0;
-            }  
+            }
         }
 
         fixed_rate[j] = get_bit_num(maxQuant);
@@ -149,38 +146,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -191,29 +195,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -275,7 +269,7 @@
                              (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                              (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                              (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                              (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                              (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -296,33 +290,30 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_1D_fixed_f32(float* const __restrict__ decData, 
-                                                  const unsigned char* const __restrict__ cmpData, 
-                                                  volatile unsigned int* const __restrict__ cmpOffset, 
-                                                  volatile unsigned int* const __restrict__ locOffset, 
-                                                  volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_1D_fixed_f32(float* const __restrict__ decData,
+                                                  const unsigned char* const __restrict__ cmpData,
+                                                  volatile unsigned int* const __restrict__ cmpOffset,
+                                                  volatile unsigned int* const __restrict__ locOffset,
+                                                  volatile int* const __restrict__ flag,
                                                   const float eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
-    int block_idx;    
+    int block_idx;
     int absQuant[32];
     int currQuant;
     int sign_ofs;
@@ -347,38 +338,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -389,24 +387,17 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            flag[bid] = 2;
+            __threadfence();
         }
-        __syncthreads();
     }
-    
-    if(warp>0)
-    {
-        if(!lane) cmpOffset[warp] = excl_sum;
-        __threadfence();
-        if(!lane) flag[warp] = 2;
-        __threadfence();  
-    }
-    __syncthreads();
-
-    if(!lane) base_idx = excl_sum + rate_ofs;
     __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -436,7 +427,7 @@
                         (0x0000ff00 & (tmp_char.z << 8))  |
                         (0x000000ff & tmp_char.w);
             cmp_byte_ofs+=4;
-            
+
             for(int i=0; i<32; i++) absQuant[i] = 0;
             for(int i=0; i<fixed_rate[j]; i++)
             {
@@ -479,7 +470,7 @@
                 absQuant[30] |= ((tmp_char.w >> 1) & 0x00000001) << i;
                 absQuant[31] |= ((tmp_char.w >> 0) & 0x00000001) << i;
             }
-            
+
             if(base_block_end_idx < nbEle)
             {
                 #pragma unroll 8
@@ -500,7 +491,7 @@
                     sign_ofs = (i+3) % 32;
                     currQuant = sign_flag & (1 << (31 - sign_ofs)) ? absQuant[i+3] * -1 : absQuant[i+3];
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<float4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                 }
             }
@@ -512,7 +503,7 @@
                     currQuant = sign_flag & (1 << (31 - sign_ofs)) ? absQuant[i] * -1 : absQuant[i];
                     if(base_block_start_idx+i < nbEle) decData[base_block_start_idx+i] = currQuant * eb * 2;
                 }
-            }      
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -520,30 +511,27 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_1D_plain_f32(const float* const __restrict__ oriData, 
-                                                unsigned char* const __restrict__ cmpData, 
-                                                volatile unsigned int* const __restrict__ cmpOffset, 
-                                                volatile unsigned int* const __restrict__ locOffset, 
-                                                volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_1D_plain_f32(const float* const __restrict__ oriData,
+                                                unsigned char* const __restrict__ cmpData,
+                                                volatile unsigned int* const __restrict__ cmpOffset,
+                                                volatile unsigned int* const __restrict__ locOffset,
+                                                volatile int* const __restrict__ flag,
                                                 const float eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
     const float recipPrecision = 0.5f/eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
@@ -635,7 +623,7 @@
 
                 quant_chunk_idx = j * 32 + nbEle % 32;
                 for(int i=quant_chunk_idx; i<quant_chunk_idx+zeronbEle; i++) absQuant[i] = 0;
-            }  
+            }
         }
 
         fixed_rate[j] = get_bit_num(maxQuant);
@@ -652,38 +640,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -694,29 +689,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -778,7 +763,7 @@
                              (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                              (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                              (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                              (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                              (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -799,33 +784,30 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_1D_plain_f32(float* const __restrict__ decData, 
-                                                  const unsigned char* const __restrict__ cmpData, 
-                                                  volatile unsigned int* const __restrict__ cmpOffset, 
-                                                  volatile unsigned int* const __restrict__ locOffset, 
-                                                  volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_1D_plain_f32(float* const __restrict__ decData,
+                                                  const unsigned char* const __restrict__ cmpData,
+                                                  volatile unsigned int* const __restrict__ cmpOffset,
+                                                  volatile unsigned int* const __restrict__ locOffset,
+                                                  volatile int* const __restrict__ flag,
                                                   const float eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
-    int block_idx;    
+    int block_idx;
     int absQuant[32];
     int currQuant, lorenQuant, prevQuant;
     int sign_ofs;
@@ -850,38 +832,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -892,24 +881,17 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            flag[bid] = 2;
+            __threadfence();
         }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane) cmpOffset[warp] = excl_sum;
-        __threadfence();
-        if(!lane) flag[warp] = 2;
-        __threadfence();  
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -939,7 +921,7 @@
                         (0x0000ff00 & (tmp_char.z << 8))  |
                         (0x000000ff & tmp_char.w);
             cmp_byte_ofs+=4;
-            
+
             for(int i=0; i<32; i++) absQuant[i] = 0;
             for(int i=0; i<fixed_rate[j]; i++)
             {
@@ -982,7 +964,7 @@
                 absQuant[30] |= ((tmp_char.w >> 1) & 0x00000001) << i;
                 absQuant[31] |= ((tmp_char.w >> 0) & 0x00000001) << i;
             }
-            
+
             prevQuant = 0;
             if(base_block_end_idx < nbEle)
             {
@@ -1012,7 +994,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<float4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                 }
             }
@@ -1026,7 +1008,7 @@
                     prevQuant = currQuant;
                     if(base_block_start_idx+i < nbEle) decData[base_block_start_idx+i] = currQuant * eb * 2;
                 }
-            }      
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -1034,30 +1016,27 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_1D_outlier_f32(const float* const __restrict__ oriData, 
-                                                    unsigned char* const __restrict__ cmpData, 
-                                                    volatile unsigned int* const __restrict__ cmpOffset, 
-                                                    volatile unsigned int* const __restrict__ locOffset, 
-                                                    volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_1D_outlier_f32(const float* const __restrict__ oriData,
+                                                    unsigned char* const __restrict__ cmpData,
+                                                    volatile unsigned int* const __restrict__ cmpOffset,
+                                                    volatile unsigned int* const __restrict__ locOffset,
+                                                    volatile int* const __restrict__ flag,
                                                     const float eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
     const float recipPrecision = 0.5f/eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
@@ -1159,7 +1138,7 @@
 
                 quant_chunk_idx = j * 32 + nbEle % 32;
                 for(int i=quant_chunk_idx; i<quant_chunk_idx+zeronbEle; i++) absQuant[i] = 0;
-            }  
+            }
         }
 
         int fr1 = get_bit_num(maxQuant);
@@ -1168,12 +1147,12 @@
         int temp_rate = 0;
         int temp_ofs1 = fr1 ? 4 + fr1 * 4 : 0;
         int temp_ofs2 = fr2 ? 4 + fr2 * 4 + outlier: 4 + outlier;
-        if(temp_ofs1<=temp_ofs2) 
+        if(temp_ofs1<=temp_ofs2)
         {
             thread_ofs += temp_ofs1;
             temp_rate = fr1;
         }
-        else 
+        else
         {
             thread_ofs += temp_ofs2;
             temp_rate = fr2 | 0x80 | ((outlier-1) << 5);
@@ -1192,38 +1171,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1234,29 +1220,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
-        } 
+        }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1317,7 +1293,7 @@
                     tmp_char.w = 0;
 
                     if(!encoding_selection) tmp_char.x = (((absQuant[chunk_idx_start] & mask) >> i) << 7);
-                    tmp_char.x = tmp_char.x | 
+                    tmp_char.x = tmp_char.x |
                                 (((absQuant[chunk_idx_start+0] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+1] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+2] & mask) >> i) << 5) |
@@ -1344,7 +1320,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1371,7 +1347,7 @@
                 tmp_char.w = 0;
                 tmp_char.x = 0xff & sign_flag[j];
                 if(!encoding_selection) tmp_char.y = ((absQuant[chunk_idx_start] & 1) << 7);
-                tmp_char.y = tmp_char.y | 
+                tmp_char.y = tmp_char.y |
                             ((absQuant[chunk_idx_start+1] & 1) << 6) |
                             ((absQuant[chunk_idx_start+2] & 1) << 5) |
                             ((absQuant[chunk_idx_start+3] & 1) << 4) |
@@ -1405,7 +1381,7 @@
                     tmp_char.y = 0;
                     tmp_char.z = 0;
                     tmp_char.w = 0;
-                    
+
                     tmp_char.x = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1417,7 +1393,7 @@
                     mask <<= 1;
 
                     if(!encoding_selection) tmp_char.y = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
-                    tmp_char.y = tmp_char.y | 
+                    tmp_char.y = tmp_char.y |
                                 (((absQuant[chunk_idx_start+0] & mask) >> (i+1)) << 7) |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
                                 (((absQuant[chunk_idx_start+2] & mask) >> (i+1)) << 5) |
@@ -1470,7 +1446,7 @@
                 tmp_char.x = 0xff & (sign_flag[j] >> 8);
                 tmp_char.y = 0xff & sign_flag[j];
                 if(!encoding_selection) tmp_char.z = ((absQuant[chunk_idx_start] & 1) << 7);
-                tmp_char.z = tmp_char.z | 
+                tmp_char.z = tmp_char.z |
                             ((absQuant[chunk_idx_start+1] & 1) << 6) |
                             ((absQuant[chunk_idx_start+2] & 1) << 5) |
                             ((absQuant[chunk_idx_start+3] & 1) << 4) |
@@ -1505,7 +1481,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char.y = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1517,7 +1493,7 @@
                     mask <<= 1;
 
                     if(!encoding_selection) tmp_char.z = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
-                    tmp_char.z = tmp_char.z | 
+                    tmp_char.z = tmp_char.z |
                                 (((absQuant[chunk_idx_start+0] & mask) >> (i+1)) << 7) |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
                                 (((absQuant[chunk_idx_start+2] & mask) >> (i+1)) << 5) |
@@ -1569,7 +1545,7 @@
                 tmp_char.y = 0xff & (sign_flag[j] >> 8);
                 tmp_char.z = 0xff & sign_flag[j];
                 if(!encoding_selection) tmp_char.w = ((absQuant[chunk_idx_start] & 1) << 7);
-                tmp_char.w = tmp_char.w | 
+                tmp_char.w = tmp_char.w |
                             ((absQuant[chunk_idx_start+1] & 1) << 6) |
                             ((absQuant[chunk_idx_start+2] & 1) << 5) |
                             ((absQuant[chunk_idx_start+3] & 1) << 4) |
@@ -1605,7 +1581,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char.z = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1656,39 +1632,36 @@
                                         (((absQuant[chunk_idx_start+31] & mask) >> (fixed_rate[j]-1)) << 0);
             }
         }
-        
+
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
     }
 }
 
 
-__global__ void cuSZp_decompress_kernel_1D_outlier_f32(float* const __restrict__ decData, 
-                                                    const unsigned char* const __restrict__ cmpData, 
-                                                    volatile unsigned int* const __restrict__ cmpOffset, 
-                                                    volatile unsigned int* const __restrict__ locOffset, 
-                                                    volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_1D_outlier_f32(float* const __restrict__ decData,
+                                                    const unsigned char* const __restrict__ cmpData,
+                                                    volatile unsigned int* const __restrict__ cmpOffset,
+                                                    volatile unsigned int* const __restrict__ locOffset,
+                                                    volatile int* const __restrict__ flag,
                                                     const float eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
-    int block_idx;    
+    int block_idx;
     int absQuant[32];
     int currQuant, lorenQuant, prevQuant;
     int sign_ofs;
@@ -1718,38 +1691,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1760,24 +1740,17 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            flag[bid] = 2;
+            __threadfence();
         }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane) cmpOffset[warp] = excl_sum;
-        __threadfence();
-        if(!lane) flag[warp] = 2;
-        __threadfence();  
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1850,7 +1823,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.w = currQuant * eb * 2;
-                        
+
                         reinterpret_cast<float4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                     }
                 }
@@ -2090,7 +2063,7 @@
                     absQuant[12] |= ((tmp_char.w >> 3) & 0x00000001) << (i+1);
                     absQuant[13] |= ((tmp_char.w >> 2) & 0x00000001) << (i+1);
                     absQuant[14] |= ((tmp_char.w >> 1) & 0x00000001) << (i+1);
-                    absQuant[15] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);                    
+                    absQuant[15] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);
                 }
 
                 int uchar_buffer = cmpData[cmp_byte_ofs++];
@@ -2119,7 +2092,7 @@
 
                 if(encoding_selection) absQuant[0] = outlier_buffer;
 
-                sign_flag = (0xff000000 & cmpData[cmp_byte_ofs++] << 24);                            
+                sign_flag = (0xff000000 & cmpData[cmp_byte_ofs++] << 24);
 
                 tmp_char = reinterpret_cast<const uchar4*>(cmpData)[cmp_byte_ofs/4];
                 sign_flag |= (0x00ff0000 & tmp_char.x << 16) |
@@ -2175,7 +2148,7 @@
                     absQuant[4] |= ((tmp_char.w >> 3) & 0x00000001) << (i+1);
                     absQuant[5] |= ((tmp_char.w >> 2) & 0x00000001) << (i+1);
                     absQuant[6] |= ((tmp_char.w >> 1) & 0x00000001) << (i+1);
-                    absQuant[7] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);                    
+                    absQuant[7] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);
                 }
 
                 int uchar_buffer = cmpData[cmp_byte_ofs++];
@@ -2208,7 +2181,7 @@
                 absQuant[30] |= ((uchar_buffer >> 1) & 0x00000001) << (fixed_rate[j]-1);
                 absQuant[31] |= ((uchar_buffer >> 0) & 0x00000001) << (fixed_rate[j]-1);
             }
-            
+
             prevQuant = 0;
             if(base_block_end_idx < nbEle)
             {
@@ -2238,7 +2211,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<float4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                 }
             }
diff -ruN a/src/cuSZp_kernels_1D_f64.cu b/src/cuSZp_kernels_1D_f64.cu
--- a/src/cuSZp_kernels_1D_f64.cu	2025-10-24 21:38:31.000000000 +0000
+++ b/src/cuSZp_kernels_1D_f64.cu	2026-07-22 01:18:32.460663887 +0000
@@ -13,30 +13,27 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_1D_fixed_f64(const double* const __restrict__ oriData, 
-                                                unsigned char* const __restrict__ cmpData, 
-                                                volatile unsigned int* const __restrict__ cmpOffset, 
-                                                volatile unsigned int* const __restrict__ locOffset, 
-                                                volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_1D_fixed_f64(const double* const __restrict__ oriData,
+                                                unsigned char* const __restrict__ cmpData,
+                                                volatile unsigned int* const __restrict__ cmpOffset,
+                                                volatile unsigned int* const __restrict__ locOffset,
+                                                volatile int* const __restrict__ flag,
                                                 const double eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
     const double recipPrecision = 0.5f/eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
@@ -117,7 +114,7 @@
 
                 quant_chunk_idx = j * 32 + nbEle % 32;
                 for(int i=quant_chunk_idx; i<quant_chunk_idx+zeronbEle; i++) absQuant[i] = 0;
-            }  
+            }
         }
 
         fixed_rate[j] = get_bit_num(maxQuant);
@@ -134,38 +131,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -176,29 +180,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -260,7 +254,7 @@
                              (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                              (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                              (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                              (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                              (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -281,33 +275,30 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_1D_fixed_f64(double* const __restrict__ decData, 
-                                                    const unsigned char* const __restrict__ cmpData, 
-                                                    volatile unsigned int* const __restrict__ cmpOffset, 
-                                                    volatile unsigned int* const __restrict__ locOffset, 
-                                                    volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_1D_fixed_f64(double* const __restrict__ decData,
+                                                    const unsigned char* const __restrict__ cmpData,
+                                                    volatile unsigned int* const __restrict__ cmpOffset,
+                                                    volatile unsigned int* const __restrict__ locOffset,
+                                                    volatile int* const __restrict__ flag,
                                                     const double eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
-    int block_idx;    
+    int block_idx;
     int absQuant[32];
     int currQuant;
     int sign_ofs;
@@ -332,38 +323,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -374,24 +372,17 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            flag[bid] = 2;
+            __threadfence();
         }
-        __syncthreads();
     }
-    
-    if(warp>0)
-    {
-        if(!lane) cmpOffset[warp] = excl_sum;
-        __threadfence();
-        if(!lane) flag[warp] = 2;
-        __threadfence();  
-    }
-    __syncthreads();
-
-    if(!lane) base_idx = excl_sum + rate_ofs;
     __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -421,7 +412,7 @@
                         (0x0000ff00 & (tmp_char.z << 8))  |
                         (0x000000ff & tmp_char.w);
             cmp_byte_ofs+=4;
-            
+
             for(int i=0; i<32; i++) absQuant[i] = 0;
             for(int i=0; i<fixed_rate[j]; i++)
             {
@@ -464,7 +455,7 @@
                 absQuant[30] |= ((tmp_char.w >> 1) & 0x00000001) << i;
                 absQuant[31] |= ((tmp_char.w >> 0) & 0x00000001) << i;
             }
-            
+
             if(base_block_end_idx < nbEle)
             {
                 #pragma unroll 8
@@ -485,7 +476,7 @@
                     sign_ofs = (i+3) % 32;
                     currQuant = sign_flag & (1 << (31 - sign_ofs)) ? absQuant[i+3] * -1 : absQuant[i+3];
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<double4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                 }
             }
@@ -497,7 +488,7 @@
                     currQuant = sign_flag & (1 << (31 - sign_ofs)) ? absQuant[i] * -1 : absQuant[i];
                     if(base_block_start_idx+i < nbEle) decData[base_block_start_idx+i] = currQuant * eb * 2;
                 }
-            }      
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -505,30 +496,27 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_1D_plain_f64(const double* const __restrict__ oriData, 
-                                                unsigned char* const __restrict__ cmpData, 
-                                                volatile unsigned int* const __restrict__ cmpOffset, 
-                                                volatile unsigned int* const __restrict__ locOffset, 
-                                                volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_1D_plain_f64(const double* const __restrict__ oriData,
+                                                unsigned char* const __restrict__ cmpData,
+                                                volatile unsigned int* const __restrict__ cmpOffset,
+                                                volatile unsigned int* const __restrict__ locOffset,
+                                                volatile int* const __restrict__ flag,
                                                 const double eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
     const double recipPrecision = 0.5f/eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
@@ -620,7 +608,7 @@
 
                 quant_chunk_idx = j * 32 + nbEle % 32;
                 for(int i=quant_chunk_idx; i<quant_chunk_idx+zeronbEle; i++) absQuant[i] = 0;
-            }  
+            }
         }
 
         fixed_rate[j] = get_bit_num(maxQuant);
@@ -637,38 +625,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -679,29 +674,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -763,7 +748,7 @@
                              (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                              (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                              (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                              (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                              (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -784,33 +769,30 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_1D_plain_f64(double* const __restrict__ decData, 
-                                                    const unsigned char* const __restrict__ cmpData, 
-                                                    volatile unsigned int* const __restrict__ cmpOffset, 
-                                                    volatile unsigned int* const __restrict__ locOffset, 
-                                                    volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_1D_plain_f64(double* const __restrict__ decData,
+                                                    const unsigned char* const __restrict__ cmpData,
+                                                    volatile unsigned int* const __restrict__ cmpOffset,
+                                                    volatile unsigned int* const __restrict__ locOffset,
+                                                    volatile int* const __restrict__ flag,
                                                     const double eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
-    int block_idx;    
+    int block_idx;
     int absQuant[32];
     int currQuant, lorenQuant, prevQuant;
     int sign_ofs;
@@ -835,38 +817,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -877,24 +866,17 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            flag[bid] = 2;
+            __threadfence();
         }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane) cmpOffset[warp] = excl_sum;
-        __threadfence();
-        if(!lane) flag[warp] = 2;
-        __threadfence();  
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -924,7 +906,7 @@
                         (0x0000ff00 & (tmp_char.z << 8))  |
                         (0x000000ff & tmp_char.w);
             cmp_byte_ofs+=4;
-            
+
             for(int i=0; i<32; i++) absQuant[i] = 0;
             for(int i=0; i<fixed_rate[j]; i++)
             {
@@ -967,7 +949,7 @@
                 absQuant[30] |= ((tmp_char.w >> 1) & 0x00000001) << i;
                 absQuant[31] |= ((tmp_char.w >> 0) & 0x00000001) << i;
             }
-            
+
             prevQuant = 0;
             if(base_block_end_idx < nbEle)
             {
@@ -997,7 +979,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<double4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                 }
             }
@@ -1011,7 +993,7 @@
                     prevQuant = currQuant;
                     if(base_block_start_idx+i < nbEle) decData[base_block_start_idx+i] = currQuant * eb * 2;
                 }
-            }      
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -1019,30 +1001,27 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_1D_outlier_f64(const double* const __restrict__ oriData, 
-                                                    unsigned char* const __restrict__ cmpData, 
-                                                    volatile unsigned int* const __restrict__ cmpOffset, 
-                                                    volatile unsigned int* const __restrict__ locOffset, 
-                                                    volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_1D_outlier_f64(const double* const __restrict__ oriData,
+                                                    unsigned char* const __restrict__ cmpData,
+                                                    volatile unsigned int* const __restrict__ cmpOffset,
+                                                    volatile unsigned int* const __restrict__ locOffset,
+                                                    volatile int* const __restrict__ flag,
                                                     const double eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
     const double recipPrecision = 0.5f/eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
@@ -1144,7 +1123,7 @@
 
                 quant_chunk_idx = j * 32 + nbEle % 32;
                 for(int i=quant_chunk_idx; i<quant_chunk_idx+zeronbEle; i++) absQuant[i] = 0;
-            }  
+            }
         }
 
         int fr1 = get_bit_num(maxQuant);
@@ -1153,12 +1132,12 @@
         int temp_rate = 0;
         int temp_ofs1 = fr1 ? 4 + fr1 * 4 : 0;
         int temp_ofs2 = fr2 ? 4 + fr2 * 4 + outlier: 4 + outlier;
-        if(temp_ofs1<=temp_ofs2) 
+        if(temp_ofs1<=temp_ofs2)
         {
             thread_ofs += temp_ofs1;
             temp_rate = fr1;
         }
-        else 
+        else
         {
             thread_ofs += temp_ofs2;
             temp_rate = fr2 | 0x80 | ((outlier-1) << 5);
@@ -1177,38 +1156,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1219,29 +1205,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
-        } 
+        }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1302,7 +1278,7 @@
                     tmp_char.w = 0;
 
                     if(!encoding_selection) tmp_char.x = (((absQuant[chunk_idx_start] & mask) >> i) << 7);
-                    tmp_char.x = tmp_char.x | 
+                    tmp_char.x = tmp_char.x |
                                 (((absQuant[chunk_idx_start+0] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+1] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+2] & mask) >> i) << 5) |
@@ -1329,7 +1305,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1356,7 +1332,7 @@
                 tmp_char.w = 0;
                 tmp_char.x = 0xff & sign_flag[j];
                 if(!encoding_selection) tmp_char.y = ((absQuant[chunk_idx_start] & 1) << 7);
-                tmp_char.y = tmp_char.y | 
+                tmp_char.y = tmp_char.y |
                             ((absQuant[chunk_idx_start+1] & 1) << 6) |
                             ((absQuant[chunk_idx_start+2] & 1) << 5) |
                             ((absQuant[chunk_idx_start+3] & 1) << 4) |
@@ -1390,7 +1366,7 @@
                     tmp_char.y = 0;
                     tmp_char.z = 0;
                     tmp_char.w = 0;
-                    
+
                     tmp_char.x = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1402,7 +1378,7 @@
                     mask <<= 1;
 
                     if(!encoding_selection) tmp_char.y = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
-                    tmp_char.y = tmp_char.y | 
+                    tmp_char.y = tmp_char.y |
                                 (((absQuant[chunk_idx_start+0] & mask) >> (i+1)) << 7) |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
                                 (((absQuant[chunk_idx_start+2] & mask) >> (i+1)) << 5) |
@@ -1455,7 +1431,7 @@
                 tmp_char.x = 0xff & (sign_flag[j] >> 8);
                 tmp_char.y = 0xff & sign_flag[j];
                 if(!encoding_selection) tmp_char.z = ((absQuant[chunk_idx_start] & 1) << 7);
-                tmp_char.z = tmp_char.z | 
+                tmp_char.z = tmp_char.z |
                             ((absQuant[chunk_idx_start+1] & 1) << 6) |
                             ((absQuant[chunk_idx_start+2] & 1) << 5) |
                             ((absQuant[chunk_idx_start+3] & 1) << 4) |
@@ -1490,7 +1466,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char.y = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1502,7 +1478,7 @@
                     mask <<= 1;
 
                     if(!encoding_selection) tmp_char.z = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
-                    tmp_char.z = tmp_char.z | 
+                    tmp_char.z = tmp_char.z |
                                 (((absQuant[chunk_idx_start+0] & mask) >> (i+1)) << 7) |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
                                 (((absQuant[chunk_idx_start+2] & mask) >> (i+1)) << 5) |
@@ -1554,7 +1530,7 @@
                 tmp_char.y = 0xff & (sign_flag[j] >> 8);
                 tmp_char.z = 0xff & sign_flag[j];
                 if(!encoding_selection) tmp_char.w = ((absQuant[chunk_idx_start] & 1) << 7);
-                tmp_char.w = tmp_char.w | 
+                tmp_char.w = tmp_char.w |
                             ((absQuant[chunk_idx_start+1] & 1) << 6) |
                             ((absQuant[chunk_idx_start+2] & 1) << 5) |
                             ((absQuant[chunk_idx_start+3] & 1) << 4) |
@@ -1590,7 +1566,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char.z = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1641,39 +1617,36 @@
                                         (((absQuant[chunk_idx_start+31] & mask) >> (fixed_rate[j]-1)) << 0);
             }
         }
-        
+
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
     }
 }
 
 
-__global__ void cuSZp_decompress_kernel_1D_outlier_f64(double* const __restrict__ decData, 
-                                                    const unsigned char* const __restrict__ cmpData, 
-                                                    volatile unsigned int* const __restrict__ cmpOffset, 
-                                                    volatile unsigned int* const __restrict__ locOffset, 
-                                                    volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_1D_outlier_f64(double* const __restrict__ decData,
+                                                    const unsigned char* const __restrict__ cmpData,
+                                                    volatile unsigned int* const __restrict__ cmpOffset,
+                                                    volatile unsigned int* const __restrict__ locOffset,
+                                                    volatile int* const __restrict__ flag,
                                                     const double eb, const size_t nbEle)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const int tid = threadIdx.x;
     const int bid = blockIdx.x;
     const int idx = bid * blockDim.x + tid;
     const int lane = idx & 0x1f;
     const int warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const int block_num = thread_chunk >> 5;
     const int rate_ofs = (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     int base_start_idx;
     int base_block_start_idx, base_block_end_idx;
-    int block_idx;    
+    int block_idx;
     int absQuant[32];
     int currQuant, lorenQuant, prevQuant;
     int sign_ofs;
@@ -1703,38 +1676,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1745,24 +1725,17 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            flag[bid] = 2;
+            __threadfence();
         }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane) cmpOffset[warp] = excl_sum;
-        __threadfence();
-        if(!lane) flag[warp] = 2;
-        __threadfence();  
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1835,7 +1808,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.w = currQuant * eb * 2;
-                        
+
                         reinterpret_cast<double4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                     }
                 }
@@ -2075,7 +2048,7 @@
                     absQuant[12] |= ((tmp_char.w >> 3) & 0x00000001) << (i+1);
                     absQuant[13] |= ((tmp_char.w >> 2) & 0x00000001) << (i+1);
                     absQuant[14] |= ((tmp_char.w >> 1) & 0x00000001) << (i+1);
-                    absQuant[15] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);                    
+                    absQuant[15] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);
                 }
 
                 int uchar_buffer = cmpData[cmp_byte_ofs++];
@@ -2104,7 +2077,7 @@
 
                 if(encoding_selection) absQuant[0] = outlier_buffer;
 
-                sign_flag = (0xff000000 & cmpData[cmp_byte_ofs++] << 24);                            
+                sign_flag = (0xff000000 & cmpData[cmp_byte_ofs++] << 24);
 
                 tmp_char = reinterpret_cast<const uchar4*>(cmpData)[cmp_byte_ofs/4];
                 sign_flag |= (0x00ff0000 & tmp_char.x << 16) |
@@ -2160,7 +2133,7 @@
                     absQuant[4] |= ((tmp_char.w >> 3) & 0x00000001) << (i+1);
                     absQuant[5] |= ((tmp_char.w >> 2) & 0x00000001) << (i+1);
                     absQuant[6] |= ((tmp_char.w >> 1) & 0x00000001) << (i+1);
-                    absQuant[7] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);                    
+                    absQuant[7] |= ((tmp_char.w >> 0) & 0x00000001) << (i+1);
                 }
 
                 int uchar_buffer = cmpData[cmp_byte_ofs++];
@@ -2193,7 +2166,7 @@
                 absQuant[30] |= ((uchar_buffer >> 1) & 0x00000001) << (fixed_rate[j]-1);
                 absQuant[31] |= ((uchar_buffer >> 0) & 0x00000001) << (fixed_rate[j]-1);
             }
-            
+
             prevQuant = 0;
             if(base_block_end_idx < nbEle)
             {
@@ -2223,7 +2196,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<double4*>(decData)[(base_block_start_idx+i)/4] = dec_buffer;
                 }
             }
diff -ruN a/src/cuSZp_kernels_2D_f32.cu b/src/cuSZp_kernels_2D_f32.cu
--- a/src/cuSZp_kernels_2D_f32.cu	2025-10-24 21:38:31.000000000 +0000
+++ b/src/cuSZp_kernels_2D_f32.cu	2026-07-22 01:18:32.462453551 +0000
@@ -28,31 +28,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_2D_fixed_vec4_f32(const float* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_2D_fixed_vec4_f32(const float* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
     const float recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -80,7 +77,7 @@
             unsigned int maxQuant = 0;
             int currQuant;
             uint data_idx_z = block_idx_z;
-            
+
             #pragma unroll 8
             for(uint i=0; i<8; i++)
             {
@@ -88,35 +85,35 @@
                 uint data_idx_x = block_idx_x * 8;
                 uint data_idx = data_idx_z * dims.y * dims.x + data_idx_y * dims.x + data_idx_x;
                 quant_chunk_idx = j * 64 + i * 8;
-                
+
                 if(data_idx_y < dims.y)
                 {
-                    float4 tmp_buffer = reinterpret_cast<const float4*>(oriData)[data_idx/4]; 
-                    
+                    float4 tmp_buffer = reinterpret_cast<const float4*>(oriData)[data_idx/4];
+
                     currQuant = quantization(tmp_buffer.x, recipPrecision);
                     sign_ofs = (i * 8) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                    
+
                     currQuant = quantization(tmp_buffer.y, recipPrecision);
                     sign_ofs = (i * 8 + 1) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+1] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                    
+
                     currQuant = quantization(tmp_buffer.z, recipPrecision);
                     sign_ofs = (i * 8 + 2) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+2] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                    
+
                     currQuant = quantization(tmp_buffer.w, recipPrecision);
                     sign_ofs = (i * 8 + 3) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+3] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+3] ? maxQuant : absQuant[quant_chunk_idx+3];
-                    
+
                     if(data_idx_x + 4 < dims.x)
                     {
                         tmp_buffer = reinterpret_cast<const float4*>(oriData)[(data_idx+4)/4];
@@ -126,19 +123,19 @@
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+4] = abs(currQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+4] ? maxQuant : absQuant[quant_chunk_idx+4];
-                        
+
                         currQuant = quantization(tmp_buffer.y, recipPrecision);
                         sign_ofs = (i * 8 + 5) % 32;
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+5] = abs(currQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+5] ? maxQuant : absQuant[quant_chunk_idx+5];
-                        
+
                         currQuant = quantization(tmp_buffer.z, recipPrecision);
                         sign_ofs = (i * 8 + 6) % 32;
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+6] = abs(currQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+6] ? maxQuant : absQuant[quant_chunk_idx+6];
-                        
+
                         currQuant = quantization(tmp_buffer.w, recipPrecision);
                         sign_ofs = (i * 8 + 7) % 32;
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
@@ -174,38 +171,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -216,29 +220,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -304,7 +298,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -340,7 +334,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -349,7 +343,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -362,30 +356,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_2D_fixed_vec4_f32(float* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_2D_fixed_vec4_f32(float* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -416,38 +407,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -458,29 +456,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -625,7 +613,7 @@
                     sign_ofs = (i * 8 + 3) % 32;
                     currQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+3] * -1 : absQuant[i*8+3];
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
 
                     if(data_idx_x + 4 < dims.x)
@@ -633,7 +621,7 @@
                         sign_ofs = (i * 8 + 4) % 32;
                         currQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+4] * -1 : absQuant[i*8+4];
                         dec_buffer.x = currQuant * eb * 2;
-                        
+
                         sign_ofs = (i * 8 + 5) % 32;
                         currQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                         dec_buffer.y = currQuant * eb * 2;
@@ -649,7 +637,7 @@
                         reinterpret_cast<float4*>(decData)[(data_idx+4)/4] = dec_buffer;
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -657,31 +645,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_2D_plain_vec4_f32(const float* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_2D_plain_vec4_f32(const float* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
     const float recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -710,7 +695,7 @@
             int currQuant, lorenQuant;
             int prevQuant_y = 0;
             uint data_idx_z = block_idx_z;
-            
+
             #pragma unroll 8
             for(uint i=0; i<8; i++)
             {
@@ -719,11 +704,11 @@
                 uint data_idx = data_idx_z * dims.y * dims.x + data_idx_y * dims.x + data_idx_x;
                 quant_chunk_idx = j * 64 + i * 8;
                 int prevQuant = 0;
-                
+
                 if(data_idx_y < dims.y)
                 {
-                    float4 tmp_buffer = reinterpret_cast<const float4*>(oriData)[data_idx/4]; 
-                    
+                    float4 tmp_buffer = reinterpret_cast<const float4*>(oriData)[data_idx/4];
+
                     currQuant = quantization(tmp_buffer.x, recipPrecision);
                     lorenQuant = currQuant - prevQuant_y;
                     prevQuant_y = currQuant;
@@ -732,7 +717,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                    
+
                     currQuant = quantization(tmp_buffer.y, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -740,7 +725,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                    
+
                     currQuant = quantization(tmp_buffer.z, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -748,7 +733,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                    
+
                     currQuant = quantization(tmp_buffer.w, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -756,7 +741,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+3] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+3] ? maxQuant : absQuant[quant_chunk_idx+3];
-                    
+
                     if(data_idx_x + 4 < dims.x)
                     {
                         tmp_buffer = reinterpret_cast<const float4*>(oriData)[(data_idx+4)/4];
@@ -768,7 +753,7 @@
                         sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+4] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+4] ? maxQuant : absQuant[quant_chunk_idx+4];
-                        
+
                         currQuant = quantization(tmp_buffer.y, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -776,7 +761,7 @@
                         sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+5] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+5] ? maxQuant : absQuant[quant_chunk_idx+5];
-                        
+
                         currQuant = quantization(tmp_buffer.z, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -784,7 +769,7 @@
                         sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+6] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+6] ? maxQuant : absQuant[quant_chunk_idx+6];
-                        
+
                         currQuant = quantization(tmp_buffer.w, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -822,38 +807,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -864,29 +856,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -952,7 +934,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -988,7 +970,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -997,7 +979,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -1010,30 +992,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_2D_plain_vec4_f32(float* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_2D_plain_vec4_f32(float* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -1064,38 +1043,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1106,29 +1092,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1284,7 +1260,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
 
                     if(data_idx_x + 4 < dims.x)
@@ -1294,7 +1270,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.x = currQuant * eb * 2;
-                        
+
                         sign_ofs = (i * 8 + 5) % 32;
                         lorenQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                         currQuant = lorenQuant + prevQuant;
@@ -1316,7 +1292,7 @@
                         reinterpret_cast<float4*>(decData)[(data_idx+4)/4] = dec_buffer;
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -1324,31 +1300,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_2D_outlier_vec4_f32(const float* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_2D_outlier_vec4_f32(const float* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
     const float recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -1379,7 +1352,7 @@
             int outlier = 0;
             int prevQuant_y = 0;
             uint data_idx_z = block_idx_z;
-            
+
             #pragma unroll 8
             for(uint i=0; i<8; i++)
             {
@@ -1388,11 +1361,11 @@
                 uint data_idx = data_idx_z * dims.y * dims.x + data_idx_y * dims.x + data_idx_x;
                 quant_chunk_idx = j * 64 + i * 8;
                 int prevQuant = 0;
-                
+
                 if(data_idx_y < dims.y)
                 {
-                    float4 tmp_buffer = reinterpret_cast<const float4*>(oriData)[data_idx/4]; 
-                    
+                    float4 tmp_buffer = reinterpret_cast<const float4*>(oriData)[data_idx/4];
+
                     currQuant = quantization(tmp_buffer.x, recipPrecision);
                     lorenQuant = currQuant - prevQuant_y;
                     prevQuant_y = currQuant;
@@ -1403,7 +1376,7 @@
                     maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
                     if(i) maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx] ? maxQuan2 : absQuant[quant_chunk_idx];
                     else outlier = absQuant[quant_chunk_idx];
-                    
+
                     currQuant = quantization(tmp_buffer.y, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -1412,7 +1385,7 @@
                     absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
                     maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+1] ? maxQuan2 : absQuant[quant_chunk_idx+1];
-                    
+
                     currQuant = quantization(tmp_buffer.z, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -1421,7 +1394,7 @@
                     absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
                     maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+2] ? maxQuan2 : absQuant[quant_chunk_idx+2];
-                    
+
                     currQuant = quantization(tmp_buffer.w, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -1430,7 +1403,7 @@
                     absQuant[quant_chunk_idx+3] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+3] ? maxQuant : absQuant[quant_chunk_idx+3];
                     maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+3] ? maxQuan2 : absQuant[quant_chunk_idx+3];
-                    
+
                     if(data_idx_x + 4 < dims.x)
                     {
                         tmp_buffer = reinterpret_cast<const float4*>(oriData)[(data_idx+4)/4];
@@ -1443,7 +1416,7 @@
                         absQuant[quant_chunk_idx+4] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+4] ? maxQuant : absQuant[quant_chunk_idx+4];
                         maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+4] ? maxQuan2 : absQuant[quant_chunk_idx+4];
-                        
+
                         currQuant = quantization(tmp_buffer.y, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -1452,7 +1425,7 @@
                         absQuant[quant_chunk_idx+5] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+5] ? maxQuant : absQuant[quant_chunk_idx+5];
                         maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+5] ? maxQuan2 : absQuant[quant_chunk_idx+5];
-                        
+
                         currQuant = quantization(tmp_buffer.z, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -1461,7 +1434,7 @@
                         absQuant[quant_chunk_idx+6] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+6] ? maxQuant : absQuant[quant_chunk_idx+6];
                         maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+6] ? maxQuan2 : absQuant[quant_chunk_idx+6];
-                        
+
                         currQuant = quantization(tmp_buffer.w, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -1491,12 +1464,12 @@
             int temp_rate = 0;
             int temp_ofs1 = fr1 ? 8 + fr1 * 8 : 0;
             int temp_ofs2 = fr2 ? 8 + fr2 * 8 + outlier: 8 + outlier;
-            if(temp_ofs1<=temp_ofs2) 
+            if(temp_ofs1<=temp_ofs2)
             {
                 thread_ofs += temp_ofs1;
                 temp_rate = fr1;
             }
-            else 
+            else
             {
                 thread_ofs += temp_ofs2;
                 temp_rate = fr2 | 0x80 | ((outlier-1) << 5);
@@ -1516,38 +1489,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1558,29 +1538,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1611,7 +1581,7 @@
                 cmpData[cmp_byte_ofs++] = (unsigned char)(absQuant[chunk_idx_start] & 0xff);
                 absQuant[chunk_idx_start] >>= 8;
             }
-            
+
             if(!fixed_rate[j])
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
@@ -1675,7 +1645,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1711,7 +1681,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1720,7 +1690,7 @@
                                 (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1732,7 +1702,7 @@
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 8);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & sign_flag[2*j];
                 tmp_char1.y = 0xff & (sign_flag[2*j+1] >> 24);
@@ -1767,7 +1737,7 @@
                             ((absQuant[chunk_idx_start+21] & 1) << 2) |
                             ((absQuant[chunk_idx_start+22] & 1) << 1) |
                             ((absQuant[chunk_idx_start+23] & 1) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -1813,7 +1783,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.x = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1823,7 +1793,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.y = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.y = tmp_char2.y |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -1851,7 +1821,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1894,10 +1864,10 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1911,7 +1881,7 @@
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 8);
                 tmp_char1.y = 0xff & sign_flag[2*j];
@@ -1993,7 +1963,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.y = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2003,7 +1973,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.z = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.z = tmp_char2.z |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -2022,7 +1992,7 @@
                                 (((absQuant[chunk_idx_start+13] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+14] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+15] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -2065,7 +2035,7 @@
                             (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
 
@@ -2077,7 +2047,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2090,7 +2060,7 @@
             else
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 16);
                 tmp_char1.y = 0xff & (sign_flag[2*j] >> 8);
@@ -2173,7 +2143,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.z = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2183,7 +2153,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.w = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.w = tmp_char2.w |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -2193,7 +2163,7 @@
                                 (((absQuant[chunk_idx_start+5] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+6] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+7] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -2248,7 +2218,7 @@
                                         (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+48] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+49] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+50] & mask) >> i) << 5) |
@@ -2257,7 +2227,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2274,30 +2244,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_2D_outlier_vec4_f32(float* const __restrict__ decData, 
-                                                            const unsigned char* const __restrict__ cmpData, 
-                                                            volatile unsigned int* const __restrict__ cmpOffset, 
-                                                            volatile unsigned int* const __restrict__ locOffset, 
-                                                            volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_2D_outlier_vec4_f32(float* const __restrict__ decData,
+                                                            const unsigned char* const __restrict__ cmpData,
+                                                            volatile unsigned int* const __restrict__ cmpOffset,
+                                                            volatile unsigned int* const __restrict__ locOffset,
+                                                            volatile int* const __restrict__ flag,
                                                             uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -2333,38 +2300,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -2375,29 +2349,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -2486,7 +2450,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.w = currQuant * eb * 2;
-                        
+
                         reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
 
                         if(data_idx_x + 4 < dims.x)
@@ -2496,7 +2460,7 @@
                             currQuant = lorenQuant + prevQuant;
                             prevQuant = currQuant;
                             dec_buffer.x = currQuant * eb * 2;
-                            
+
                             sign_ofs = (i * 8 + 5) % 32;
                             lorenQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                             currQuant = lorenQuant + prevQuant;
@@ -2800,7 +2764,7 @@
             else if(vec_ofs==2)
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
 
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24)) |
@@ -2976,9 +2940,9 @@
             else
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
-                
+
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24));
 
                 tmp_char1 = reinterpret_cast<const uchar4*>(cmpData)[cmp_byte_ofs/4];
@@ -3189,7 +3153,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
 
                     if(data_idx_x + 4 < dims.x)
@@ -3199,7 +3163,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.x = currQuant * eb * 2;
-                        
+
                         sign_ofs = (i * 8 + 5) % 32;
                         lorenQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                         currQuant = lorenQuant + prevQuant;
@@ -3221,7 +3185,7 @@
                         reinterpret_cast<float4*>(decData)[(data_idx+4)/4] = dec_buffer;
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
diff -ruN a/src/cuSZp_kernels_2D_f64.cu b/src/cuSZp_kernels_2D_f64.cu
--- a/src/cuSZp_kernels_2D_f64.cu	2025-10-24 21:38:31.000000000 +0000
+++ b/src/cuSZp_kernels_2D_f64.cu	2026-07-22 01:18:32.464580789 +0000
@@ -13,31 +13,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_2D_fixed_vec4_f64(const double* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_2D_fixed_vec4_f64(const double* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
     const double recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -65,7 +62,7 @@
             unsigned int maxQuant = 0;
             int currQuant;
             uint data_idx_z = block_idx_z;
-            
+
             #pragma unroll 8
             for(uint i=0; i<8; i++)
             {
@@ -73,35 +70,35 @@
                 uint data_idx_x = block_idx_x * 8;
                 uint data_idx = data_idx_z * dims.y * dims.x + data_idx_y * dims.x + data_idx_x;
                 quant_chunk_idx = j * 64 + i * 8;
-                
+
                 if(data_idx_y < dims.y)
                 {
-                    double4 tmp_buffer = reinterpret_cast<const double4*>(oriData)[data_idx/4]; 
-                    
+                    double4 tmp_buffer = reinterpret_cast<const double4*>(oriData)[data_idx/4];
+
                     currQuant = quantization(tmp_buffer.x, recipPrecision);
                     sign_ofs = (i * 8) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                    
+
                     currQuant = quantization(tmp_buffer.y, recipPrecision);
                     sign_ofs = (i * 8 + 1) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+1] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                    
+
                     currQuant = quantization(tmp_buffer.z, recipPrecision);
                     sign_ofs = (i * 8 + 2) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+2] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                    
+
                     currQuant = quantization(tmp_buffer.w, recipPrecision);
                     sign_ofs = (i * 8 + 3) % 32;
                     sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+3] = abs(currQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+3] ? maxQuant : absQuant[quant_chunk_idx+3];
-                    
+
                     if(data_idx_x + 4 < dims.x)
                     {
                         tmp_buffer = reinterpret_cast<const double4*>(oriData)[(data_idx+4)/4];
@@ -111,19 +108,19 @@
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+4] = abs(currQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+4] ? maxQuant : absQuant[quant_chunk_idx+4];
-                        
+
                         currQuant = quantization(tmp_buffer.y, recipPrecision);
                         sign_ofs = (i * 8 + 5) % 32;
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+5] = abs(currQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+5] ? maxQuant : absQuant[quant_chunk_idx+5];
-                        
+
                         currQuant = quantization(tmp_buffer.z, recipPrecision);
                         sign_ofs = (i * 8 + 6) % 32;
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+6] = abs(currQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+6] ? maxQuant : absQuant[quant_chunk_idx+6];
-                        
+
                         currQuant = quantization(tmp_buffer.w, recipPrecision);
                         sign_ofs = (i * 8 + 7) % 32;
                         sign_flag[2*j+i/4] |= (currQuant < 0) << (31 - sign_ofs);
@@ -159,38 +156,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -201,29 +205,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -289,7 +283,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -325,7 +319,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -334,7 +328,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -347,30 +341,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_2D_fixed_vec4_f64(double* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_2D_fixed_vec4_f64(double* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -401,38 +392,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -443,29 +441,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -610,7 +598,7 @@
                     sign_ofs = (i * 8 + 3) % 32;
                     currQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+3] * -1 : absQuant[i*8+3];
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
 
                     if(data_idx_x + 4 < dims.x)
@@ -618,7 +606,7 @@
                         sign_ofs = (i * 8 + 4) % 32;
                         currQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+4] * -1 : absQuant[i*8+4];
                         dec_buffer.x = currQuant * eb * 2;
-                        
+
                         sign_ofs = (i * 8 + 5) % 32;
                         currQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                         dec_buffer.y = currQuant * eb * 2;
@@ -634,7 +622,7 @@
                         reinterpret_cast<double4*>(decData)[(data_idx+4)/4] = dec_buffer;
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -642,31 +630,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_2D_plain_vec4_f64(const double* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_2D_plain_vec4_f64(const double* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
     const double recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -695,7 +680,7 @@
             int currQuant, lorenQuant;
             int prevQuant_y = 0;
             uint data_idx_z = block_idx_z;
-            
+
             #pragma unroll 8
             for(uint i=0; i<8; i++)
             {
@@ -704,11 +689,11 @@
                 uint data_idx = data_idx_z * dims.y * dims.x + data_idx_y * dims.x + data_idx_x;
                 quant_chunk_idx = j * 64 + i * 8;
                 int prevQuant = 0;
-                
+
                 if(data_idx_y < dims.y)
                 {
-                    double4 tmp_buffer = reinterpret_cast<const double4*>(oriData)[data_idx/4]; 
-                    
+                    double4 tmp_buffer = reinterpret_cast<const double4*>(oriData)[data_idx/4];
+
                     currQuant = quantization(tmp_buffer.x, recipPrecision);
                     lorenQuant = currQuant - prevQuant_y;
                     prevQuant_y = currQuant;
@@ -717,7 +702,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                    
+
                     currQuant = quantization(tmp_buffer.y, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -725,7 +710,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                    
+
                     currQuant = quantization(tmp_buffer.z, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -733,7 +718,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                    
+
                     currQuant = quantization(tmp_buffer.w, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -741,7 +726,7 @@
                     sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                     absQuant[quant_chunk_idx+3] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+3] ? maxQuant : absQuant[quant_chunk_idx+3];
-                    
+
                     if(data_idx_x + 4 < dims.x)
                     {
                         tmp_buffer = reinterpret_cast<const double4*>(oriData)[(data_idx+4)/4];
@@ -753,7 +738,7 @@
                         sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+4] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+4] ? maxQuant : absQuant[quant_chunk_idx+4];
-                        
+
                         currQuant = quantization(tmp_buffer.y, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -761,7 +746,7 @@
                         sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+5] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+5] ? maxQuant : absQuant[quant_chunk_idx+5];
-                        
+
                         currQuant = quantization(tmp_buffer.z, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -769,7 +754,7 @@
                         sign_flag[2*j+i/4] |= (lorenQuant < 0) << (31 - sign_ofs);
                         absQuant[quant_chunk_idx+6] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+6] ? maxQuant : absQuant[quant_chunk_idx+6];
-                        
+
                         currQuant = quantization(tmp_buffer.w, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -807,38 +792,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -849,29 +841,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -937,7 +919,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -973,7 +955,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -982,7 +964,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -995,30 +977,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_2D_plain_vec4_f64(double* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_2D_plain_vec4_f64(double* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -1049,38 +1028,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1091,29 +1077,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1269,7 +1245,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
 
                     if(data_idx_x + 4 < dims.x)
@@ -1279,7 +1255,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.x = currQuant * eb * 2;
-                        
+
                         sign_ofs = (i * 8 + 5) % 32;
                         lorenQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                         currQuant = lorenQuant + prevQuant;
@@ -1301,7 +1277,7 @@
                         reinterpret_cast<double4*>(decData)[(data_idx+4)/4] = dec_buffer;
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -1309,31 +1285,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_2D_outlier_vec4_f64(const double* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_2D_outlier_vec4_f64(const double* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
     const double recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -1364,7 +1337,7 @@
             int outlier = 0;
             int prevQuant_y = 0;
             uint data_idx_z = block_idx_z;
-            
+
             #pragma unroll 8
             for(uint i=0; i<8; i++)
             {
@@ -1373,11 +1346,11 @@
                 uint data_idx = data_idx_z * dims.y * dims.x + data_idx_y * dims.x + data_idx_x;
                 quant_chunk_idx = j * 64 + i * 8;
                 int prevQuant = 0;
-                
+
                 if(data_idx_y < dims.y)
                 {
-                    double4 tmp_buffer = reinterpret_cast<const double4*>(oriData)[data_idx/4]; 
-                    
+                    double4 tmp_buffer = reinterpret_cast<const double4*>(oriData)[data_idx/4];
+
                     currQuant = quantization(tmp_buffer.x, recipPrecision);
                     lorenQuant = currQuant - prevQuant_y;
                     prevQuant_y = currQuant;
@@ -1388,7 +1361,7 @@
                     maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
                     if(i) maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx] ? maxQuan2 : absQuant[quant_chunk_idx];
                     else outlier = absQuant[quant_chunk_idx];
-                    
+
                     currQuant = quantization(tmp_buffer.y, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -1397,7 +1370,7 @@
                     absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
                     maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+1] ? maxQuan2 : absQuant[quant_chunk_idx+1];
-                    
+
                     currQuant = quantization(tmp_buffer.z, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -1406,7 +1379,7 @@
                     absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
                     maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+2] ? maxQuan2 : absQuant[quant_chunk_idx+2];
-                    
+
                     currQuant = quantization(tmp_buffer.w, recipPrecision);
                     lorenQuant = currQuant - prevQuant;
                     prevQuant = currQuant;
@@ -1415,7 +1388,7 @@
                     absQuant[quant_chunk_idx+3] = abs(lorenQuant);
                     maxQuant = maxQuant > absQuant[quant_chunk_idx+3] ? maxQuant : absQuant[quant_chunk_idx+3];
                     maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+3] ? maxQuan2 : absQuant[quant_chunk_idx+3];
-                    
+
                     if(data_idx_x + 4 < dims.x)
                     {
                         tmp_buffer = reinterpret_cast<const double4*>(oriData)[(data_idx+4)/4];
@@ -1428,7 +1401,7 @@
                         absQuant[quant_chunk_idx+4] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+4] ? maxQuant : absQuant[quant_chunk_idx+4];
                         maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+4] ? maxQuan2 : absQuant[quant_chunk_idx+4];
-                        
+
                         currQuant = quantization(tmp_buffer.y, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -1437,7 +1410,7 @@
                         absQuant[quant_chunk_idx+5] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+5] ? maxQuant : absQuant[quant_chunk_idx+5];
                         maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+5] ? maxQuan2 : absQuant[quant_chunk_idx+5];
-                        
+
                         currQuant = quantization(tmp_buffer.z, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -1446,7 +1419,7 @@
                         absQuant[quant_chunk_idx+6] = abs(lorenQuant);
                         maxQuant = maxQuant > absQuant[quant_chunk_idx+6] ? maxQuant : absQuant[quant_chunk_idx+6];
                         maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+6] ? maxQuan2 : absQuant[quant_chunk_idx+6];
-                        
+
                         currQuant = quantization(tmp_buffer.w, recipPrecision);
                         lorenQuant = currQuant - prevQuant;
                         prevQuant = currQuant;
@@ -1476,12 +1449,12 @@
             int temp_rate = 0;
             int temp_ofs1 = fr1 ? 8 + fr1 * 8 : 0;
             int temp_ofs2 = fr2 ? 8 + fr2 * 8 + outlier: 8 + outlier;
-            if(temp_ofs1<=temp_ofs2) 
+            if(temp_ofs1<=temp_ofs2)
             {
                 thread_ofs += temp_ofs1;
                 temp_rate = fr1;
             }
-            else 
+            else
             {
                 thread_ofs += temp_ofs2;
                 temp_rate = fr2 | 0x80 | ((outlier-1) << 5);
@@ -1501,38 +1474,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1543,29 +1523,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1596,7 +1566,7 @@
                 cmpData[cmp_byte_ofs++] = (unsigned char)(absQuant[chunk_idx_start] & 0xff);
                 absQuant[chunk_idx_start] >>= 8;
             }
-            
+
             if(!fixed_rate[j])
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
@@ -1660,7 +1630,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1696,7 +1666,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1705,7 +1675,7 @@
                                 (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1717,7 +1687,7 @@
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 8);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & sign_flag[2*j];
                 tmp_char1.y = 0xff & (sign_flag[2*j+1] >> 24);
@@ -1752,7 +1722,7 @@
                             ((absQuant[chunk_idx_start+21] & 1) << 2) |
                             ((absQuant[chunk_idx_start+22] & 1) << 1) |
                             ((absQuant[chunk_idx_start+23] & 1) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -1798,7 +1768,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.x = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1808,7 +1778,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.y = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.y = tmp_char2.y |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -1836,7 +1806,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1879,10 +1849,10 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1896,7 +1866,7 @@
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 8);
                 tmp_char1.y = 0xff & sign_flag[2*j];
@@ -1978,7 +1948,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.y = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1988,7 +1958,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.z = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.z = tmp_char2.z |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -2007,7 +1977,7 @@
                                 (((absQuant[chunk_idx_start+13] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+14] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+15] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -2050,7 +2020,7 @@
                             (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
 
@@ -2062,7 +2032,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2075,7 +2045,7 @@
             else
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 16);
                 tmp_char1.y = 0xff & (sign_flag[2*j] >> 8);
@@ -2158,7 +2128,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.z = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2168,7 +2138,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.w = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.w = tmp_char2.w |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -2178,7 +2148,7 @@
                                 (((absQuant[chunk_idx_start+5] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+6] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+7] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -2233,7 +2203,7 @@
                                         (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+48] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+49] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+50] & mask) >> i) << 5) |
@@ -2242,7 +2212,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2259,30 +2229,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_2D_outlier_vec4_f64(double* const __restrict__ decData, 
-                                                            const unsigned char* const __restrict__ cmpData, 
-                                                            volatile unsigned int* const __restrict__ cmpOffset, 
-                                                            volatile unsigned int* const __restrict__ locOffset, 
-                                                            volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_2D_outlier_vec4_f64(double* const __restrict__ decData,
+                                                            const unsigned char* const __restrict__ cmpData,
+                                                            volatile unsigned int* const __restrict__ cmpOffset,
+                                                            volatile unsigned int* const __restrict__ locOffset,
+                                                            volatile int* const __restrict__ flag,
                                                             uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 7) / 8;
     const uint dimxBlock = (dims.x + 7) / 8;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -2318,38 +2285,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -2360,29 +2334,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -2471,7 +2435,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.w = currQuant * eb * 2;
-                        
+
                         reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
 
                         if(data_idx_x + 4 < dims.x)
@@ -2481,7 +2445,7 @@
                             currQuant = lorenQuant + prevQuant;
                             prevQuant = currQuant;
                             dec_buffer.x = currQuant * eb * 2;
-                            
+
                             sign_ofs = (i * 8 + 5) % 32;
                             lorenQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                             currQuant = lorenQuant + prevQuant;
@@ -2785,7 +2749,7 @@
             else if(vec_ofs==2)
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
 
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24)) |
@@ -2961,9 +2925,9 @@
             else
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
-                
+
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24));
 
                 tmp_char1 = reinterpret_cast<const uchar4*>(cmpData)[cmp_byte_ofs/4];
@@ -3174,7 +3138,7 @@
                     currQuant = lorenQuant + prevQuant;
                     prevQuant = currQuant;
                     dec_buffer.w = currQuant * eb * 2;
-                    
+
                     reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
 
                     if(data_idx_x + 4 < dims.x)
@@ -3184,7 +3148,7 @@
                         currQuant = lorenQuant + prevQuant;
                         prevQuant = currQuant;
                         dec_buffer.x = currQuant * eb * 2;
-                        
+
                         sign_ofs = (i * 8 + 5) % 32;
                         lorenQuant = sign_flag[i/4] & (1 << (31 - sign_ofs)) ? absQuant[i*8+5] * -1 : absQuant[i*8+5];
                         currQuant = lorenQuant + prevQuant;
@@ -3206,7 +3170,7 @@
                         reinterpret_cast<double4*>(decData)[(data_idx+4)/4] = dec_buffer;
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
diff -ruN a/src/cuSZp_kernels_3D_f32.cu b/src/cuSZp_kernels_3D_f32.cu
--- a/src/cuSZp_kernels_3D_f32.cu	2025-10-24 21:38:31.000000000 +0000
+++ b/src/cuSZp_kernels_3D_f32.cu	2026-07-22 01:18:32.465799563 +0000
@@ -29,31 +29,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_3D_fixed_vec4_f32(const float* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_3D_fixed_vec4_f32(const float* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
     const float recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -84,7 +81,7 @@
             for(uint i=0; i<4; i++)
             {
                 uint data_idx_z = block_idx_z * 4 + i;
-                
+
                 if(data_idx_z < dims.z)
                 {
                     for(uint k=0; k<4; k++)
@@ -104,19 +101,19 @@
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx] = abs(currQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                            
+
                             currQuant = quantization(tmp_buffer.y, recipPrecision);
                             sign_ofs = (block_ofs + 1) % 32;
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+1] = abs(currQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                            
+
                             currQuant = quantization(tmp_buffer.z, recipPrecision);
                             sign_ofs = (block_ofs + 2) % 32;
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+2] = abs(currQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                            
+
                             currQuant = quantization(tmp_buffer.w, recipPrecision);
                             sign_ofs = (block_ofs + 3) % 32;
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
@@ -154,38 +151,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -196,29 +200,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -284,7 +278,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -320,7 +314,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -329,7 +323,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -341,30 +335,27 @@
     }
 }
 
-__global__ void cuSZp_decompress_kernel_3D_fixed_vec4_f32(float* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_3D_fixed_vec4_f32(float* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -395,38 +386,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -437,29 +435,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -610,12 +598,12 @@
                             sign_ofs = (block_ofs + 3) % 32;
                             currQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs+3] * -1 : absQuant[block_ofs+3];
                             dec_buffer.w = currQuant * eb * 2;
-                            
+
                             reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
                         }
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -623,31 +611,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_3D_plain_vec4_f32(const float* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_3D_plain_vec4_f32(const float* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
     const float recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -680,7 +665,7 @@
             {
                 uint data_idx_z = block_idx_z * 4 + i;
                 int prevQuant_y = 0;
-                
+
                 if(data_idx_z < dims.z)
                 {
                     for(uint k=0; k<4; k++)
@@ -708,7 +693,7 @@
                             sign_flag[2*j+i/2] |= (lorenQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                            
+
                             currQuant = quantization(tmp_buffer.y, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -716,7 +701,7 @@
                             sign_flag[2*j+i/2] |= (lorenQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                            
+
                             currQuant = quantization(tmp_buffer.z, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -724,7 +709,7 @@
                             sign_flag[2*j+i/2] |= (lorenQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                            
+
                             currQuant = quantization(tmp_buffer.w, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -764,38 +749,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -806,29 +798,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -894,7 +876,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -930,7 +912,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -939,7 +921,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -951,30 +933,27 @@
     }
 }
 
-__global__ void cuSZp_decompress_kernel_3D_plain_vec4_f32(float* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_3D_plain_vec4_f32(float* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -1005,38 +984,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1047,29 +1033,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1211,7 +1187,7 @@
                             sign_ofs = block_ofs % 32;
                             lorenQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs] * -1 : absQuant[block_ofs];
                             if(k) currQuant = lorenQuant + prevQuant_y; // Y-de-delta
-                            else { 
+                            else {
                                 currQuant = lorenQuant + prevQuant_z;   // Z-de-delta
                                 prevQuant_z = currQuant;
                             }
@@ -1236,12 +1212,12 @@
                             currQuant = lorenQuant + prevQuant_x;
                             prevQuant_x = currQuant;
                             dec_buffer.w = currQuant * eb * 2;
-                            
+
                             reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
                         }
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -1249,31 +1225,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_3D_outlier_vec4_f32(const float* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_3D_outlier_vec4_f32(const float* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
     const float recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -1308,7 +1281,7 @@
             {
                 uint data_idx_z = block_idx_z * 4 + i;
                 int prevQuant_y = 0;
-                
+
                 if(data_idx_z < dims.z)
                 {
                     for(uint k=0; k<4; k++)
@@ -1338,7 +1311,7 @@
                             maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
                             if(i||k) maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx] ? maxQuan2 : absQuant[quant_chunk_idx];
                             else outlier = absQuant[quant_chunk_idx];
-                            
+
                             currQuant = quantization(tmp_buffer.y, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -1347,7 +1320,7 @@
                             absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
                             maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+1] ? maxQuan2 : absQuant[quant_chunk_idx+1];
-                            
+
                             currQuant = quantization(tmp_buffer.z, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -1356,7 +1329,7 @@
                             absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
                             maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+2] ? maxQuan2 : absQuant[quant_chunk_idx+2];
-                            
+
                             currQuant = quantization(tmp_buffer.w, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -1388,12 +1361,12 @@
             int temp_rate = 0;
             int temp_ofs1 = fr1 ? 8 + fr1 * 8 : 0;
             int temp_ofs2 = fr2 ? 8 + fr2 * 8 + outlier: 8 + outlier;
-            if(temp_ofs1<=temp_ofs2) 
+            if(temp_ofs1<=temp_ofs2)
             {
                 thread_ofs += temp_ofs1;
                 temp_rate = fr1;
             }
-            else 
+            else
             {
                 thread_ofs += temp_ofs2;
                 temp_rate = fr2 | 0x80 | ((outlier-1) << 5);
@@ -1413,38 +1386,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1455,29 +1435,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1508,7 +1478,7 @@
                 cmpData[cmp_byte_ofs++] = (unsigned char)(absQuant[chunk_idx_start] & 0xff);
                 absQuant[chunk_idx_start] >>= 8;
             }
-            
+
             if(!fixed_rate[j])
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
@@ -1572,7 +1542,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1608,7 +1578,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1617,7 +1587,7 @@
                                 (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1629,7 +1599,7 @@
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 8);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & sign_flag[2*j];
                 tmp_char1.y = 0xff & (sign_flag[2*j+1] >> 24);
@@ -1664,7 +1634,7 @@
                             ((absQuant[chunk_idx_start+21] & 1) << 2) |
                             ((absQuant[chunk_idx_start+22] & 1) << 1) |
                             ((absQuant[chunk_idx_start+23] & 1) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -1710,7 +1680,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.x = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1720,7 +1690,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.y = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.y = tmp_char2.y |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -1748,7 +1718,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1791,10 +1761,10 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1808,7 +1778,7 @@
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 8);
                 tmp_char1.y = 0xff & sign_flag[2*j];
@@ -1890,7 +1860,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.y = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1900,7 +1870,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.z = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.z = tmp_char2.z |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -1919,7 +1889,7 @@
                                 (((absQuant[chunk_idx_start+13] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+14] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+15] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1962,7 +1932,7 @@
                             (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
 
@@ -1974,7 +1944,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1987,7 +1957,7 @@
             else
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 16);
                 tmp_char1.y = 0xff & (sign_flag[2*j] >> 8);
@@ -2070,7 +2040,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.z = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2080,7 +2050,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.w = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.w = tmp_char2.w |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -2090,7 +2060,7 @@
                                 (((absQuant[chunk_idx_start+5] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+6] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+7] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -2145,7 +2115,7 @@
                                         (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+48] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+49] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+50] & mask) >> i) << 5) |
@@ -2154,7 +2124,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2171,30 +2141,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_3D_outlier_vec4_f32(float* const __restrict__ decData, 
-                                                            const unsigned char* const __restrict__ cmpData, 
-                                                            volatile unsigned int* const __restrict__ cmpOffset, 
-                                                            volatile unsigned int* const __restrict__ locOffset, 
-                                                            volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_3D_outlier_vec4_f32(float* const __restrict__ decData,
+                                                            const unsigned char* const __restrict__ cmpData,
+                                                            volatile unsigned int* const __restrict__ cmpOffset,
+                                                            volatile unsigned int* const __restrict__ locOffset,
+                                                            volatile int* const __restrict__ flag,
                                                             uint blockNum, const uint3 dims, const float eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -2230,38 +2197,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -2272,29 +2246,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -2344,7 +2308,7 @@
                                (0x000000ff & cmpData[cmp_byte_ofs++]);
                 absQuant[0] = outlier_buffer;
                 for(uint i=1; i<64; i++) absQuant[i] = 0;
-                
+
                 int currQuant, lorenQuant;
                 int prevQuant_z = 0;
                 for(uint i=0; i<4; i++)
@@ -2369,7 +2333,7 @@
                                 sign_ofs = block_ofs % 32;
                                 lorenQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs] * -1 : absQuant[block_ofs];
                                 if(k) currQuant = lorenQuant + prevQuant_y; // Y-de-delta
-                                else { 
+                                else {
                                     currQuant = lorenQuant + prevQuant_z;   // Z-de-delta
                                     prevQuant_z = currQuant;
                                 }
@@ -2394,7 +2358,7 @@
                                 currQuant = lorenQuant + prevQuant_x;
                                 prevQuant_x = currQuant;
                                 dec_buffer.w = currQuant * eb * 2;
-                                
+
                                 reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
                             }
                         }
@@ -2681,7 +2645,7 @@
             else if(vec_ofs==2)
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
 
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24)) |
@@ -2857,9 +2821,9 @@
             else
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
-                
+
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24));
 
                 tmp_char1 = reinterpret_cast<const uchar4*>(cmpData)[cmp_byte_ofs/4];
@@ -3056,7 +3020,7 @@
                             sign_ofs = block_ofs % 32;
                             lorenQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs] * -1 : absQuant[block_ofs];
                             if(k) currQuant = lorenQuant + prevQuant_y; // Y-de-delta
-                            else { 
+                            else {
                                 currQuant = lorenQuant + prevQuant_z;   // Z-de-delta
                                 prevQuant_z = currQuant;
                             }
@@ -3081,12 +3045,12 @@
                             currQuant = lorenQuant + prevQuant_x;
                             prevQuant_x = currQuant;
                             dec_buffer.w = currQuant * eb * 2;
-                            
+
                             reinterpret_cast<float4*>(decData)[data_idx/4] = dec_buffer;
                         }
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
diff -ruN a/src/cuSZp_kernels_3D_f64.cu b/src/cuSZp_kernels_3D_f64.cu
--- a/src/cuSZp_kernels_3D_f64.cu	2025-10-24 21:38:31.000000000 +0000
+++ b/src/cuSZp_kernels_3D_f64.cu	2026-07-22 01:18:32.467007116 +0000
@@ -13,31 +13,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_3D_fixed_vec4_f64(const double* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_3D_fixed_vec4_f64(const double* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
     const double recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -68,7 +65,7 @@
             for(uint i=0; i<4; i++)
             {
                 uint data_idx_z = block_idx_z * 4 + i;
-                
+
                 if(data_idx_z < dims.z)
                 {
                     for(uint k=0; k<4; k++)
@@ -88,19 +85,19 @@
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx] = abs(currQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                            
+
                             currQuant = quantization(tmp_buffer.y, recipPrecision);
                             sign_ofs = (block_ofs + 1) % 32;
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+1] = abs(currQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                            
+
                             currQuant = quantization(tmp_buffer.z, recipPrecision);
                             sign_ofs = (block_ofs + 2) % 32;
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+2] = abs(currQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                            
+
                             currQuant = quantization(tmp_buffer.w, recipPrecision);
                             sign_ofs = (block_ofs + 3) % 32;
                             sign_flag[2*j+i/2] |= (currQuant < 0) << (31 - sign_ofs);
@@ -138,38 +135,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -180,29 +184,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -268,7 +262,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -304,7 +298,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -313,7 +307,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -325,30 +319,27 @@
     }
 }
 
-__global__ void cuSZp_decompress_kernel_3D_fixed_vec4_f64(double* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_3D_fixed_vec4_f64(double* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -379,38 +370,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -421,29 +419,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
+            __threadfence();
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -594,12 +582,12 @@
                             sign_ofs = (block_ofs + 3) % 32;
                             currQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs+3] * -1 : absQuant[block_ofs+3];
                             dec_buffer.w = currQuant * eb * 2;
-                            
+
                             reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
                         }
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -607,31 +595,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_3D_plain_vec4_f64(const double* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_3D_plain_vec4_f64(const double* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
     const double recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -664,7 +649,7 @@
             {
                 uint data_idx_z = block_idx_z * 4 + i;
                 int prevQuant_y = 0;
-                
+
                 if(data_idx_z < dims.z)
                 {
                     for(uint k=0; k<4; k++)
@@ -692,7 +677,7 @@
                             sign_flag[2*j+i/2] |= (lorenQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
-                            
+
                             currQuant = quantization(tmp_buffer.y, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -700,7 +685,7 @@
                             sign_flag[2*j+i/2] |= (lorenQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
-                            
+
                             currQuant = quantization(tmp_buffer.z, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -708,7 +693,7 @@
                             sign_flag[2*j+i/2] |= (lorenQuant < 0) << (31 - sign_ofs);
                             absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
-                            
+
                             currQuant = quantization(tmp_buffer.w, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -748,38 +733,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -790,29 +782,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -878,7 +860,7 @@
                             (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                
+
                 tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -914,7 +896,7 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                             (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                             (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -923,7 +905,7 @@
                             (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -935,30 +917,27 @@
     }
 }
 
-__global__ void cuSZp_decompress_kernel_3D_plain_vec4_f64(double* const __restrict__ decData, 
-                                                        const unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_3D_plain_vec4_f64(double* const __restrict__ decData,
+                                                        const unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -989,38 +968,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1031,29 +1017,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1195,7 +1171,7 @@
                             sign_ofs = block_ofs % 32;
                             lorenQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs] * -1 : absQuant[block_ofs];
                             if(k) currQuant = lorenQuant + prevQuant_y; // Y-de-delta
-                            else { 
+                            else {
                                 currQuant = lorenQuant + prevQuant_z;   // Z-de-delta
                                 prevQuant_z = currQuant;
                             }
@@ -1220,12 +1196,12 @@
                             currQuant = lorenQuant + prevQuant_x;
                             prevQuant_x = currQuant;
                             dec_buffer.w = currQuant * eb * 2;
-                            
+
                             reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
                         }
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
@@ -1233,31 +1209,28 @@
 }
 
 
-__global__ void cuSZp_compress_kernel_3D_outlier_vec4_f64(const double* const __restrict__ oriData, 
-                                                        unsigned char* const __restrict__ cmpData, 
-                                                        volatile unsigned int* const __restrict__ cmpOffset, 
-                                                        volatile unsigned int* const __restrict__ locOffset, 
-                                                        volatile int* const __restrict__ flag, 
+__global__ void cuSZp_compress_kernel_3D_outlier_vec4_f64(const double* const __restrict__ oriData,
+                                                        unsigned char* const __restrict__ cmpData,
+                                                        volatile unsigned int* const __restrict__ cmpOffset,
+                                                        volatile unsigned int* const __restrict__ locOffset,
+                                                        volatile int* const __restrict__ flag,
                                                         uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
     const double recipPrecision = 0.5f / eb;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     uint quant_chunk_idx;
@@ -1292,7 +1265,7 @@
             {
                 uint data_idx_z = block_idx_z * 4 + i;
                 int prevQuant_y = 0;
-                
+
                 if(data_idx_z < dims.z)
                 {
                     for(uint k=0; k<4; k++)
@@ -1322,7 +1295,7 @@
                             maxQuant = maxQuant > absQuant[quant_chunk_idx] ? maxQuant : absQuant[quant_chunk_idx];
                             if(i||k) maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx] ? maxQuan2 : absQuant[quant_chunk_idx];
                             else outlier = absQuant[quant_chunk_idx];
-                            
+
                             currQuant = quantization(tmp_buffer.y, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -1331,7 +1304,7 @@
                             absQuant[quant_chunk_idx+1] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+1] ? maxQuant : absQuant[quant_chunk_idx+1];
                             maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+1] ? maxQuan2 : absQuant[quant_chunk_idx+1];
-                            
+
                             currQuant = quantization(tmp_buffer.z, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -1340,7 +1313,7 @@
                             absQuant[quant_chunk_idx+2] = abs(lorenQuant);
                             maxQuant = maxQuant > absQuant[quant_chunk_idx+2] ? maxQuant : absQuant[quant_chunk_idx+2];
                             maxQuan2 = maxQuan2 > absQuant[quant_chunk_idx+2] ? maxQuan2 : absQuant[quant_chunk_idx+2];
-                            
+
                             currQuant = quantization(tmp_buffer.w, recipPrecision);
                             lorenQuant = currQuant - prevQuant_x;
                             prevQuant_x = currQuant;
@@ -1372,12 +1345,12 @@
             int temp_rate = 0;
             int temp_ofs1 = fr1 ? 8 + fr1 * 8 : 0;
             int temp_ofs2 = fr2 ? 8 + fr2 * 8 + outlier: 8 + outlier;
-            if(temp_ofs1<=temp_ofs2) 
+            if(temp_ofs1<=temp_ofs2)
             {
                 thread_ofs += temp_ofs1;
                 temp_rate = fr1;
             }
-            else 
+            else
             {
                 thread_ofs += temp_ofs2;
                 temp_rate = fr2 | 0x80 | ((outlier-1) << 5);
@@ -1397,38 +1370,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
         {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
+        {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -1439,29 +1419,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
 
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
-
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -1492,7 +1462,7 @@
                 cmpData[cmp_byte_ofs++] = (unsigned char)(absQuant[chunk_idx_start] & 0xff);
                 absQuant[chunk_idx_start] >>= 8;
             }
-            
+
             if(!fixed_rate[j])
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
@@ -1556,7 +1526,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> i) << 0);
-                    
+
                     tmp_char1.w = (((absQuant[chunk_idx_start+24] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+25] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+26] & mask) >> i) << 5) |
@@ -1592,7 +1562,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.w = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1601,7 +1571,7 @@
                                 (((absQuant[chunk_idx_start+61] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1613,7 +1583,7 @@
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 8);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & sign_flag[2*j];
                 tmp_char1.y = 0xff & (sign_flag[2*j+1] >> 24);
@@ -1648,7 +1618,7 @@
                             ((absQuant[chunk_idx_start+21] & 1) << 2) |
                             ((absQuant[chunk_idx_start+22] & 1) << 1) |
                             ((absQuant[chunk_idx_start+23] & 1) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                 cmp_byte_ofs+=8;
@@ -1694,7 +1664,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.x = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1704,7 +1674,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.y = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.y = tmp_char2.y |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -1732,7 +1702,7 @@
                                 (((absQuant[chunk_idx_start+21] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+22] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+23] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1775,10 +1745,10 @@
                             (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1792,7 +1762,7 @@
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 16);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 8);
                 tmp_char1.y = 0xff & sign_flag[2*j];
@@ -1874,7 +1844,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.y = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1884,7 +1854,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.z = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.z = tmp_char2.z |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -1903,7 +1873,7 @@
                                 (((absQuant[chunk_idx_start+13] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+14] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+15] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -1946,7 +1916,7 @@
                             (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                             (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                             (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                 cmp_byte_ofs+=4;
 
@@ -1958,7 +1928,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -1971,7 +1941,7 @@
             else
             {
                 cmpData[cmp_byte_ofs++] = 0xff & (sign_flag[2*j] >> 24);
-                
+
                 tmp_char2 = make_uchar4(0, 0, 0, 0);
                 tmp_char1.x = 0xff & (sign_flag[2*j] >> 16);
                 tmp_char1.y = 0xff & (sign_flag[2*j] >> 8);
@@ -2054,7 +2024,7 @@
                                 (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                 (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-                    
+
                     tmp_char2.z = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                 (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                 (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2064,7 +2034,7 @@
                                 (((absQuant[chunk_idx_start+62] & mask) >> i) << 1) |
                                 (((absQuant[chunk_idx_start+63] & mask) >> i) << 0);
                     mask <<= 1;
-                    
+
                     if(!encoding_selection) tmp_char2.w = (((absQuant[chunk_idx_start] & mask) >> (i+1)) << 7);
                     tmp_char2.w = tmp_char2.w |
                                 (((absQuant[chunk_idx_start+1] & mask) >> (i+1)) << 6) |
@@ -2074,7 +2044,7 @@
                                 (((absQuant[chunk_idx_start+5] & mask) >> (i+1)) << 2) |
                                 (((absQuant[chunk_idx_start+6] & mask) >> (i+1)) << 1) |
                                 (((absQuant[chunk_idx_start+7] & mask) >> (i+1)) << 0);
-                    
+
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4] = tmp_char1;
                     reinterpret_cast<uchar4*>(cmpData)[cmp_byte_ofs/4+1] = tmp_char2;
                     cmp_byte_ofs+=8;
@@ -2129,7 +2099,7 @@
                                         (((absQuant[chunk_idx_start+45] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+46] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+47] & mask) >> i) << 0);
-                
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+48] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+49] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+50] & mask) >> i) << 5) |
@@ -2138,7 +2108,7 @@
                                         (((absQuant[chunk_idx_start+53] & mask) >> i) << 2) |
                                         (((absQuant[chunk_idx_start+54] & mask) >> i) << 1) |
                                         (((absQuant[chunk_idx_start+55] & mask) >> i) << 0);
-    
+
                 cmpData[cmp_byte_ofs++] = (((absQuant[chunk_idx_start+56] & mask) >> i) << 7) |
                                         (((absQuant[chunk_idx_start+57] & mask) >> i) << 6) |
                                         (((absQuant[chunk_idx_start+58] & mask) >> i) << 5) |
@@ -2155,30 +2125,27 @@
 }
 
 
-__global__ void cuSZp_decompress_kernel_3D_outlier_vec4_f64(double* const __restrict__ decData, 
-                                                            const unsigned char* const __restrict__ cmpData, 
-                                                            volatile unsigned int* const __restrict__ cmpOffset, 
-                                                            volatile unsigned int* const __restrict__ locOffset, 
-                                                            volatile int* const __restrict__ flag, 
+__global__ void cuSZp_decompress_kernel_3D_outlier_vec4_f64(double* const __restrict__ decData,
+                                                            const unsigned char* const __restrict__ cmpData,
+                                                            volatile unsigned int* const __restrict__ cmpOffset,
+                                                            volatile unsigned int* const __restrict__ locOffset,
+                                                            volatile int* const __restrict__ flag,
                                                             uint blockNum, const uint3 dims, const double eb)
 {
-    __shared__ unsigned int excl_sum;
-    __shared__ unsigned int base_idx;
+    __shared__ unsigned int s_warp_totals[warps_per_block];
+    __shared__ unsigned int s_warp_excl[warps_per_block];
+    __shared__ unsigned int s_block_excl;
 
     const uint tid = threadIdx.x;
     const uint bid = blockIdx.x;
     const uint idx = bid * blockDim.x + tid;
     const uint lane = idx & 0x1f;
     const uint warp = idx >> 5;
+    const int warp_in_block = tid >> 5;
     const uint rate_ofs = (blockNum + 3) / 4 * 4;
     const uint dimyBlock = (dims.y + 3) / 4;
     const uint dimxBlock = (dims.x + 3) / 4;
 
-    if (!tid) {
-        excl_sum = 0;
-        base_idx = 0;
-    }
-    __syncthreads();
 
     uint base_start_block_idx;
     int absQuant[64];
@@ -2214,38 +2181,45 @@
     }
     __syncthreads();
 
-    if(lane==31) 
+    if(lane==31) s_warp_totals[warp_in_block] = thread_ofs;
+    __syncthreads();
+
+    if(warp_in_block==0 && lane==0)
     {
-        locOffset[warp+1] = thread_ofs;
-        __threadfence();
-        if(warp==0)
+        unsigned int running = 0;
+        for(int w=0; w<warps_per_block; w++)
+        {
+            s_warp_excl[w] = running;
+            running += s_warp_totals[w];
+        }
+        unsigned int block_total = running;
+
+        if(bid==0)
         {
+            locOffset[1] = block_total;
+            __threadfence();
             flag[0] = 2;
             __threadfence();
             flag[1] = 1;
             __threadfence();
+            s_block_excl = 0;
         }
         else
         {
-            flag[warp+1] = 1;
-            __threadfence();    
-        }
-    }
-    __syncthreads();
+            locOffset[bid+1] = block_total;
+            __threadfence();
+            flag[bid+1] = 1;
+            __threadfence();
 
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            int lookback = warp;
-            int loc_excl_sum = 0;
+            int lookback = bid;
+            unsigned int loc_excl_sum = 0;
             while(lookback>0)
             {
                 int status;
                 do{
                     status = flag[lookback];
-                    __threadfence();
                 } while (status==0);
+                __threadfence();
                 if(status==2)
                 {
                     loc_excl_sum += cmpOffset[lookback];
@@ -2256,29 +2230,19 @@
                 lookback--;
                 __threadfence();
             }
-            excl_sum = loc_excl_sum;
-        }
-        __syncthreads();
-    }
-    
-    if(warp>0)
-    {
-        if(!lane)
-        {
-            cmpOffset[warp] = excl_sum;
+            s_block_excl = loc_excl_sum;
+
+            cmpOffset[bid] = s_block_excl;
+            __threadfence();
+            if(bid==gridDim.x-1) cmpOffset[bid+1] = cmpOffset[bid] + locOffset[bid+1];
             __threadfence();
-            if(warp==gridDim.x-1) cmpOffset[warp+1] = cmpOffset[warp] + locOffset[warp+1];
+            flag[bid] = 2;
             __threadfence();
-            flag[warp] = 2;
-            __threadfence(); 
         }
     }
     __syncthreads();
-    
-    if(!lane) base_idx = excl_sum + rate_ofs;
-    __syncthreads();
 
-    unsigned int base_cmp_byte_ofs = base_idx;
+    unsigned int base_cmp_byte_ofs = rate_ofs + s_block_excl + s_warp_excl[warp_in_block];
     unsigned int cmp_byte_ofs;
     unsigned int tmp_byte_ofs = 0;
     unsigned int cur_byte_ofs = 0;
@@ -2328,7 +2292,7 @@
                                (0x000000ff & cmpData[cmp_byte_ofs++]);
                 absQuant[0] = outlier_buffer;
                 for(uint i=1; i<64; i++) absQuant[i] = 0;
-                
+
                 int currQuant, lorenQuant;
                 int prevQuant_z = 0;
                 for(uint i=0; i<4; i++)
@@ -2353,7 +2317,7 @@
                                 sign_ofs = block_ofs % 32;
                                 lorenQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs] * -1 : absQuant[block_ofs];
                                 if(k) currQuant = lorenQuant + prevQuant_y; // Y-de-delta
-                                else { 
+                                else {
                                     currQuant = lorenQuant + prevQuant_z;   // Z-de-delta
                                     prevQuant_z = currQuant;
                                 }
@@ -2378,7 +2342,7 @@
                                 currQuant = lorenQuant + prevQuant_x;
                                 prevQuant_x = currQuant;
                                 dec_buffer.w = currQuant * eb * 2;
-                                
+
                                 reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
                             }
                         }
@@ -2665,7 +2629,7 @@
             else if(vec_ofs==2)
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
 
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24)) |
@@ -2841,9 +2805,9 @@
             else
             {
                 for(uint i=0; i<64; i++) absQuant[i] = 0;
-                
+
                 if(encoding_selection) absQuant[0] = outlier_buffer;
-                
+
                 sign_flag[0] = (0xff000000 & (cmpData[cmp_byte_ofs++] << 24));
 
                 tmp_char1 = reinterpret_cast<const uchar4*>(cmpData)[cmp_byte_ofs/4];
@@ -3040,7 +3004,7 @@
                             sign_ofs = block_ofs % 32;
                             lorenQuant = sign_flag[i/2] & (1 << (31 - sign_ofs)) ? absQuant[block_ofs] * -1 : absQuant[block_ofs];
                             if(k) currQuant = lorenQuant + prevQuant_y; // Y-de-delta
-                            else { 
+                            else {
                                 currQuant = lorenQuant + prevQuant_z;   // Z-de-delta
                                 prevQuant_z = currQuant;
                             }
@@ -3065,12 +3029,12 @@
                             currQuant = lorenQuant + prevQuant_x;
                             prevQuant_x = currQuant;
                             dec_buffer.w = currQuant * eb * 2;
-                            
+
                             reinterpret_cast<double4*>(decData)[data_idx/4] = dec_buffer;
                         }
                     }
                 }
-            } 
+            }
         }
 
         cur_byte_ofs += __shfl_sync(0xffffffff, tmp_byte_ofs, 31);
